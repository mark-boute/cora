#

het linken tussen terms aan beide kanten van de cost functie is niet nodig na het gebruik van geq, right?

-> indirect het resultaat door: a >= b -> a - b >= 0.

## matrix interp

## max interp

May be s=used in cases where we are not strictly increasing/decreasing.

## Tuple interp

useful for:

| f(s(x)) -> f(p(s(x))), p(0) -> 0, p(s(x)) -> x
